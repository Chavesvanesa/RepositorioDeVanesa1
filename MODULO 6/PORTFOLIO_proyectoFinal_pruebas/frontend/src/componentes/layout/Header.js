import '../../styles/componentes/layout/Header.css';

const Header = (props) => {
    return (
        <Header className="header">
             <h4>ChavesVanesa</h4>

        </Header>
    );
}
    export default Header;